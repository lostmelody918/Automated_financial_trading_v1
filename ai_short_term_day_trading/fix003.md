**任務總覽：**
修復實盤模擬器 (live\_option\_simulator\_v2.py) 中的 3 個嚴重高頻交易邏輯地雷，包含「Pandas 跨時間尺度重採樣偏移」、「Tick 本地時間戳延遲污染」以及「SQLite 高頻開關死鎖 (Database Locked)」。完成後需進行系統測試與優化。

#### **📂 檔案一：live\_option\_simulator\_v2.py**

**任務 1.1：修復 15 分鐘線的「Train-Serve Skew」(訓練與實盤偏離)**

* **問題描述：** 在 train\_model.py 中，15 分鐘 K 線使用的是 closed='right', label='right'。但在實盤 live\_option\_simulator\_v2.py 中，你使用了預設的 .resample('15min') (預設為 left)。這會導致實盤生成的 15m K 線與訓練時完全偏移了 15 分鐘，AI 會收到完全錯誤的特徵！
* **修改指令：** 在實盤主迴圈生成 df\_15m\_base 時，強制加上與訓練集一模一樣的 closed 與 label 參數。
* **實作指引：**
  Python
  \# \[在 run\_live\_simulator 迴圈中修改\]
  \# 原本: df\_15m\_base \= df.set\_index('date').resample('15min').last().dropna()
  \# 改為:
  df\_15m\_base \= df.set\_index('date').resample('15min', closed='right', label='right').last().dropna()

**任務 1.2：阻絕高頻延遲導致的「K線時間戳污染」 (Local Clock Bug)**

* **問題描述：** 目前在 quote\_callback 內部，你是用 datetime.now() 來決定這筆 Tick 屬於哪一分鐘的 K 線。這在量化實戰是致命的！如果網路延遲 0.5 秒，08:59:59.900 交易所撮合的單，會被你錯誤分類到 09:00 的 K 線裡，導致開盤價與收盤價嚴重失真。
* **修改指令：** 在 quote\_callback 中，優先嘗試解析 Shioaji quote 物件內建的交易所時間 (通常為 Time 欄位)。若解析失敗，才退回使用 datetime.now() 作為備案。
* **實作指引：**
  Python
  \# \[在 quote\_callback 內部修改\]
  def quote\_callback(topic, quote):
      try:
          \# 🚀 優先獲取交易所時間 (防網路延遲)，若無則 fallback 到本地時間
          exchange\_time \= quote.get('Time')
          if exchange\_time:
              \# 假設 exchange\_time 是字串或 datetime.time 物件，需轉為 datetime
              \# (依據 API 實際格式做微調，此為安全示範)
              today\_date \= datetime.now().date()
              if isinstance(exchange\_time, str):
                  time\_obj \= datetime.strptime(exchange\_time\[:8\], "%H:%M:%S").time()
                  current\_time \= datetime.combine(today\_date, time\_obj)
              else:
                  current\_time \= datetime.now() \# Fallback
          else:
              current\_time \= datetime.now()

          current\_min \= current\_time.replace(second=0, microsecond=0)
          \# ... 寫入 fast\_tick\_cache 的邏輯不變 ...

**任務 1.3：修復 SQLite 高頻併發導致的「資料庫死鎖」 (Database Locked)**

* **問題描述：** PositionManager 在每次存取時都使用 with sqlite3.connect(...) 重新開啟連線。在 PRAGMA journal\_mode=WAL 的模式下，每秒高頻率開關連線會導致 SQLite 的檔案鎖 (File Lock) 來不及釋放，極高機率拋出 OperationalError: database is locked 並導致實盤閃退。
* **修改指令：** 將 PositionManager 改為維護「單一持久化連線」，並開啟 check\_same\_thread=False 與增加 timeout 容忍度。
* **實作指引：**
  Python
  \# \[修改 PositionManager 類別\]
  class PositionManager:
      def \_\_init\_\_(self, db\_path="position\_state.db"):
          self.db\_path \= db\_path
          \# 🚀 建立單一持久連線，解決 Database Locked
          self.conn \= sqlite3.connect(self.db\_path, check\_same\_thread=False, timeout=15)
          self.\_init\_db()
          self.state \= self.\_load\_state()

      def \_init\_db(self):
          \# 改用 self.conn 操作
          self.conn.execute("PRAGMA journal\_mode=WAL;")
          self.conn.execute('''CREATE TABLE IF NOT EXISTS ... ''')
          self.conn.commit()

      def \_save\_state(self):
          \# 改用 self.conn 操作
          self.conn.execute("DELETE FROM positions")
          self.conn.execute("INSERT INTO positions ...", (...))
          self.conn.commit()

      \# ⚠️ 請確保所有用到 sqlite3.connect 的地方都換成 self.conn

#### **📂 檔案二：delta\_gamma\_theta.py (補漏)**

**任務 2.1：補齊 BSM 定價公式的時間年化**

* **問題描述：** 歷史上一次修補中，似乎遺漏了 BSM 模型最重要的數學轉換：距離到期日 (DTE) 必須「除以 365」轉為年，否則 Theta 與 Gamma 會嚴重失真。
* **修改指令：** 在 get\_api\_based\_dte 函式結尾，確保回傳的天數經過年化處理。
* **實作指引：**
  Python
  \# \[修改 get\_api\_based\_dte 結尾\]
  \# 原本: return max(dte\_days, 0.001)
  \# 改為:
  dte\_years \= max(dte\_days, 0.001) / 365.0
  return dte\_years

### **✅ 任務完成後的系統驗證清單 (Validation Checklist)**

任務完成後，請 CLI/Agent 執行以下測試確保功能正常：

1. **資料庫讀寫壓力測試：** 在迴圈中模擬每秒呼叫 10 次 pos\_manager.update\_position()，持續 1 分鐘，確認不會噴出 database is locked 錯誤。(任務 1.3)
2. **K 線特徵對齊驗證：** 印出 df\_15m\_base.tail(1) 的 index，確認當前的 15m K 線收盤時間，是否完美對齊 00, 15, 30, 45 的倍數分鐘。(任務 1.1)
3. **希臘字母合理性測試：** 呼叫 BSM 定價函數，觀察 Theta 是否為正常的負值（如 \-2.5 到 \-15.0），而非無限趨近於 0。(任務 2.1)